<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partial/head.ejs', { title : `${title}`, cannonical:
    `${cannonical}` }) %>
  </head>
  <body>
    <div class="container">
      <div class="row mt-3">
        <div class="col">
          <h1 class="text-center">Pilih Paslon Mu</h1>
        </div>
      </div>
      <% if(data.length < 1) { %>
      <div class="row mt-3">
        <div class="col">
          <div class="jumbotron jumbotron-fluid">
            <div class="container">
              <h1 class="display-4">Tidak Ada Data PASLON</h1>
              <p class="lead">
                Tidak ada data paslon yang ada, mohon hubungi admin untuk
                menambahkan data paslon.
              </p>
            </div>
          </div>
        </div>
      </div>
      <% } else { %>
      <div class="row mt-3">
        <% data.forEach(paslon => { %>
        <div class="col col-md-3">
          <div class="card mb-3" style="height: 344px">
            <img
              src="data:<%=paslon.img.contentType%>;base64,<%=paslon.img.data.toString('base64')%>"
              class="card-img-top"
              alt="Gambar pasangan <%= paslon.ketua %> - <%= paslon.wakil %>"
            />
            <div class="card-body d-flex flex-column">
              <h5 class="card-title">Nama Pasangan</h5>
              <p class="card-text"><%= paslon.ketua %> - <%= paslon.wakil %></p>
              <button
                class="btn btn-success btn-block mt-auto btn-vote"
                data-id="<%= paslon._id %>"
              >
                Pilih
              </button>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
      <% } %>
    </div>

    <%- include('../partial/BSreq.ejs') %>
    <script src="/swal/dist/sweetalert2.all.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/landingPage.js"></script>
  </body>
</html>
